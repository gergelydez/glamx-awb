# MyGLS Romania Label Downloader

1. Copy `.env.example` to `.env.local`
2. Fill in your MyGLS credentials
3. Run:

npm install
npm run dev

Deploy to Vercel:
- Push to GitHub
- Import repo in Vercel
- Add MYGLS_USERNAME and MYGLS_PASSWORD in Environment Variables